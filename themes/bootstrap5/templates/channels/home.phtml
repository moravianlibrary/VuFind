<?php
    // Set up page title:
    $this->headTitle($this->translate('Channels'));

    // Set up breadcrumbs:
    $this->layout()->breadcrumbs = '<li class="breadcrumb-item active">' . $this->transEsc('Channels') . '</li>';
?>
<?=$this->render('channels/channelList.phtml')?>
