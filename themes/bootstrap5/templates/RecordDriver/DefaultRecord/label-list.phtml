<?php foreach ($this->driver->getLabels() as $label): ?>
  <span class="badge text-bg-secondary label-<?=$this->escapeHtmlAttr($label['class'])?>"><?=$this->transEsc($label['label'])?></span>
<?php endforeach; ?>
